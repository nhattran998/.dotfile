# Archive

Configs from the pre-Nix flat layout. Kept for reference; not applied by the flake.

| Path | Was |
| --- | --- |
| `shell/` | oh-my-zsh `.zshrc`, powerlevel10k, tmux, placeholder gitconfig |
| `terminals/` | alacritty, Windows Terminal |
| `editors/` | karabiner, zed |
| `apps/` | Cursor Windows keybindings |
| `linux-setup.sh` | imperative Ubuntu setup (replaced by `bootstrap-linux.sh`) |
